For the BackupsallSubscriptions.ps1, just run it and it will return all backup information.

For backupsbrief and BackupsFull, they only run against one subscription, so run select-azsuscription <sub name> first.